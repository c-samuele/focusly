export const initializeSocketLayer = (_server) => {
  // Placeholder deliberato: qui potremo agganciare Socket.IO
  // o un WebSocket server quando introdurremo la collaborazione realtime.
  return {
    enabled: false,
    path: '/ws',
  };
};
